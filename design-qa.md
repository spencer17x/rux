# Rux 紧凑界面与生成动效 · 2026-09-13

final result: passed

本次目标由用户最新反馈确定：原实现显得过大、笨重，loading 缺乏美感。该反馈取代上一轮对大字号位图的尺寸还原要求；这次是基于现有 Rux 的改进，不把偏离旧稿本身认定为缺陷。视觉风格仍供用户在交互预览中评审，不宣称主观审美已经获得认可。

## 基准、视口和证据

- source visual truth：本轮实际捕获的 `artifacts/compact-motion-2026-09-13/before.jpg`、`before-loading.jpg`，以及用户对尺寸、信息层级和 loading 的明确反馈。
- implementation：`http://127.0.0.1:5173/?preview=signature&motion=1`。
- 实际新界面截图：同目录 `after.jpg`、`after-waiting.jpg`、`after-completed.jpg`。
- 整体与局部比较：`comparison.html`、`comparison.jpg`、`comparison-motion.jpg`。旧版与新版确实放在同一个浏览器画面中比较；局部生成状态以原始 CSS 尺寸展示。
- 浏览器截图均为 1280 × 720 CSS 尺寸。整图按同一比例显示；同样两轮内容，旧版因尺寸过大无法完整放下，新版能够同时展示，这是本次有意改变的内容密度。局部裁切只调整位置，不单独改变两侧缩放比例。
- 额外桌面证据：`signature-desktop.png`（1440 × 1024）、`signature-narrow.png`（900 × 650）。已打开检查窄窗口截图。
- 可控 Agent 事件下的原生状态截图：`motion-waiting.png`、`motion-streaming.png`（1440 × 900）。截图在进入动画完成后捕获，输出状态由真实 renderer 消费的测试事件驱动。

## 本轮复查记录

1. **改版前：P1 尺寸和层级失衡。** 正文 21px、落款 19px、56px 发送按钮、310px 默认侧栏让辅助信息过分突出。修复为正文 15px/26px、运行信息 12px/20px、40px 发送按钮、256px 默认侧栏、52px 顶栏及更紧凑的导航行。保留用户手动调整过的侧栏宽度。
2. **改版前：P2 生成状态叠加过多。** 本轮捕获的等待画面包含转圈、文字闪动和跳点；代码中的思考/工具文字也有独立闪动。改为复用现有 Rux 位图标识的单一呼吸动效，文字保持静止。思考图标使用现有 Phosphor Sparkle；工具执行使用静态状态文字。
3. **改版后：通过。** 新截图显示正文与运行信息层级分离；1280px 页面能同时展示两轮完整内容；900px 输入栏保持单行，没有功能按钮溢出；等待、处理和完成的状态边界清晰。小字号的分区标签与等待说明已使用更清楚的辅助文字颜色。
4. **截图修正。** 第一张原生输出状态截图落在进入动画内，颜色较浅；测试改为等待短暂进入动画完成后再截图。未把过渡帧当作最终静态外观。

## 五项视觉表面

- **字体与排版**：沿用系统字体栈；正文、导航、辅助信息形成 15/13/12px 层级，配置字号仍有效。检查行高、英文模型名、Token 数字、文本省略及窄窗口换行。
- **间距与布局**：缩小顶栏、导航、新建按钮、头像及发送控件；收紧回复间距，将用户消息操作放入悬停区域，避免不可见按钮占据永久空白。保留正文阅读宽度和现有布局面板。
- **颜色与 token**：继续使用浅色界面、低饱和蓝色选中状态与灰蓝辅助文字；保留真实禁用、权限和错误状态。没有增加无意义的高饱和装饰或伪造百分比。
- **资产与图标**：继续使用仓库中的 Rux PNG 及 Phosphor 图标。没有新增仿制 SVG 或图片占位符；标识用于品牌动效，正常静态图标保持统一尺寸。
- **文案与内容**：每轮 Agent、模型、思考模式、Token 和耗时全部保留。等待提示为“正在准备回复 / 等待 Agent 首个响应”，收到任意内容后显示“正在处理”，完成后移除动态提示。不虚构检索、执行或验证阶段。

## 交互与辅助功能

- 生成动效只由当前消息的 running 状态触发；等待/已有内容两种形态切换，不更改 Agent IPC 或会话持久化逻辑。
- 状态有 role=status、中文可访问名称及 aria-live=polite；品牌图像为装饰性内容，不重复朗读。
- prefers-reduced-motion 下关闭呼吸及进入动画；文字状态仍保留。自动化明确验证动画启用和关闭两种模式。
- 空草稿发送禁用、运行时停止按钮、紧凑模式菜单、已确认的胶囊滑杆以及附件/语音能力边界保持不变。
- URL 中 `motion=1` 仅调整浏览器 mock 的事件节奏：等待 3 秒、分段输出、12 秒完成，便于重复体验；不改变生产 Agent 延迟或调用真实模型。

## 验证结果

- `pnpm test`：123 项单元测试、UI 边界检查、TypeScript、Web / Electron 构建通过。
- `pnpm exec playwright test`：17 项桌面测试通过。
- 更新思考/工具状态样式并生成安装包后，重新执行 TypeScript 和初始会话测试；等待→处理→完成、无动态效果设置及权限交互通过。
- `pnpm package`：生成本地 macOS arm64 应用 `release/mac-arm64/Rux.app`（未签名）。
- `git diff --check` 通过。未提交或推送代码。
- 窄窗口功能与原生截图已验收；未凭截图宣称完整 WCAG 合规，也未将模拟语音测试当作真实 Apple 听写授权验收。

## 实施清单

- [x] 收紧尺寸，区分正文与运行信息。
- [x] 替换主对话的叠加 loading，统一思考与工具状态的动态表达。
- [x] 保留已实现输入功能及每轮用量信息。
- [x] 验证正常/减少动态效果、生成状态切换、窄窗口。
- [x] 保存整体和局部对照，保留可交互预览。

此前的尺寸还原报告保存在本目录对应的 `artifacts/compact-motion-2026-09-13/previous-design-qa.md`，不再作为当前比例的验收基准。
