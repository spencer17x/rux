# Codex 26.903.61454 对齐交付记录

- 参考：本机 ChatGPT.app（com.openai.codex），26.903.61454 / build 8378。
- 方法：读取随包静态 UI 资源，提取尺寸、主题参数和事件规则；没有捕获参考应用实时窗口。
- [资源版本、SHA-256 与参数](reference.json)
- [详细对齐与产品差异](../../design-qa.md)

## 实现截图

截图来自 Rux 的原生 Electron E2E（隔离测试数据，2× 密度）。页面内容 1440 × 813 CSS px，PNG 为 2880 × 1626；系统绘制的交通灯不包含在页面截图中。

- [空会话与侧栏](workbench-empty.png)
- [新版模型强度弹窗](model-picker-26903.png)
- [Git 审查与终端工作区](git-review.png)

## 验证结果

- `pnpm test:unit`：24 个测试文件、96 项通过。
- `pnpm typecheck`：通过。
- 最终 `pnpm test:e2e`：11 项通过，包含新增的弹窗边界断言。
- `pnpm build:web` 与 `pnpm package`：通过；本机 macOS arm64 包未签名。
- 原生截图已逐张检查，模型弹窗无裁切，滑块轨道为浅色。

## 验证范围

覆盖菜单切换和焦点恢复、窄窗布局、推理滑块松开提交、快速模式和恢复默认、侧栏拖动及重启恢复、独立草稿、消息发送、图片输入、Git 和 PTY。E2E 使用确定性模型目录与响应，不调用真实模型账号。

首次新增滑块用例发现现有测试模型只有单一档位，已扩展测试数据。一次完整运行中出现窗口关闭和项目菜单点击超时，随后同一实现完整复跑 11/11 通过；另补充列表返回强度界面后的弹窗边界断言，并修正同帧重定位。

本地桌面包路径为 `../../release/mac-arm64/Rux.app`。此包用于本机验证，未签名。
