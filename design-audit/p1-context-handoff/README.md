# P1 Context Handoff desktop acceptance

Date: 2026-08-13

Build: unsigned macOS arm64 package at `app/release/mac-arm64/Rux.app`, launched with an isolated QA user-data directory.

## Evidence

- `01-handoff-preview.jpeg`: an imported Codex projection opens `复制为新任务`, targets the built-in Claude Code Agent/Connection, selects only persisted source messages, and explicitly omits file evidence because the source Task has no persisted Run-owned patch.
- `02-source-agent-summary.jpeg`: after an explicit source-Agent action, the editable summary is populated and visibly marked as generated by the pinned Rux Revision in a temporary invocation with no persisted native Session.
- The preview reports a deterministic bundle of 20 messages, 0 files, and 0 incomplete items.
- The optional narrative field is visibly labeled `Agent 生成摘要（可选、可编辑）`; constraints are independently editable.
- The confirmation boundary states that Rux will not call the target Agent or create a Native Session before confirmation. QA stopped at this boundary and did not create a target Task.

## Result

The packaged-app path from an imported Task to local fact preview passes. The core confirmed-handoff protocol, immutable snapshot, target Task creation, source/target relation, stale-preview rejection, and source immutability are covered by automated tests.

P1-E4 passes the packaged desktop path. The summary action has the distinct accessibility name `让来源 Agent 生成交接摘要`; the generated text remains editable/removable, facts remain independently visible, and QA stopped before `确认并创建新任务`, so no target Task or target Native Session was created.
